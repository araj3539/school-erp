import {useQuery} from "@tanstack/react-query";import {Card,CardContent,CardHeader} from "../components/ui/Card";import {Table} from "../components/ui/Table";import api from "../lib/api";
export default function StaffPage(){const{data,isLoading,isError}=useQuery({queryKey:["staff"],queryFn:async()=>{const r=await api.get("/staff?page=1&limit=100");return r.data}});return <div className="space-y-6"><div><h1 className="text-2xl font-bold text-gray-900">Staff & HR</h1><p className="mt-1 text-sm text-gray-500">Staff directory and employment foundation.</p></div><Card><CardHeader><h2 className="font-semibold">Staff Directory</h2></CardHeader><CardContent>{isLoading?<p className="text-sm text-gray-500" role="status">Loading staff...</p>:isError?<p className="text-sm text-red-600" role="alert">Unable to load staff. Please try again.</p>:<Table data={data?.data||[]} columns={[{key:"employeeId",header:"Employee ID"},{key:"firstName",header:"Name",render:(x:any)=>`${x.firstName} ${x.lastName}`},{key:"department",header:"Department"},{key:"designation",header:"Designation"},{key:"employmentType",header:"Type"},{key:"status",header:"Status"}]} keyExtractor={x=>x._id} emptyMessage="No staff records found."/>}</CardContent></Card></div>}
