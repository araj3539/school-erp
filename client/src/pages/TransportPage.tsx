import {useQuery} from "@tanstack/react-query";import {Card,CardContent,CardHeader} from "../components/ui/Card";import {Table} from "../components/ui/Table";import api from "../lib/api";
export default function TransportPage(){const{data,isLoading,isError}=useQuery({queryKey:["transport-routes"],queryFn:async()=>{const r=await api.get("/transport/routes?page=1&limit=100");return r.data}});return <div className="space-y-6"><div><h1 className="text-2xl font-bold text-gray-900">Transport</h1><p className="mt-1 text-sm text-gray-500">Routes, stops and vehicle allocation overview.</p></div><Card><CardHeader><h2 className="font-semibold">Routes</h2></CardHeader><CardContent>{isLoading?<p className="text-sm text-gray-500" role="status">Loading routes...</p>:isError?<p className="text-sm text-red-600" role="alert">Unable to load transport routes. Please try again.</p>:<Table data={data?.data||[]} columns={[{key:"code",header:"Code"},{key:"name",header:"Route"},{key:"status",header:"Status"}]} keyExtractor={x=>x._id} emptyMessage="No transport routes found."/>}</CardContent></Card></div>}
