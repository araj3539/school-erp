{
  "name": "school-erp",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix shared\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix shared && npm run build --prefix server && npm run build --prefix client",
    "test": "npm run test --prefix shared && npm run test --prefix server && npm run test --prefix client",
    "test:e2e": "npm run test:e2e --prefix server && npm run test:e2e --prefix client",
    "test:e2e:phase1": "npm run test:e2e:phase1 --prefix server",
    "lint": "npm run lint --prefix shared && npm run lint --prefix server && npm run lint --prefix client",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\"",
    "db:up": "docker-compose up -d",
    "db:down": "docker-compose down",
    "prepare": "node .husky/install.mjs"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.2.0",
    "prettier": "^3.3.0",
    "tsx": "^4.23.13",
    "turbo": "^2.0.0"
  },
  "packageManager": "npm@10.8.0",
  "workspaces": [
    "shared",
    "server",
    "client"
  ]
}
